<h1>投稿一覧</h1>

<% @lists.each do |list| %>

  <p>タイトル</p>
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  
  <p>画像</p>
  <% if list.image.attached? %>
    <%= image_tag list.image, size: "200×200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200×200" %>
  <% end %>
   <!--listに紐ついているimageをimage_tagに渡すことで画像を表示する-->
<% end %>

<!--@listsの中身は、先ほどコントローラのindexアクションで代入した内容がそのまま入っています。-->
<!--@lists.eachと記述することで、Listレコードの数だけ繰り返し処理を行います。-->
<!--listという変数に格納されます-->
<!--list.[カラム名]　list.titleと記述することで、投稿のタイトル（レコードのカラムの内容）が表示-->